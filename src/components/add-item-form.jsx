import Button from "./button";

export default function AddItemForm() {
    return <form >
        <h2>Add an item</h2>
        <input

            autoFocus
        />
        <Button>Add to list</Button>
    </form>
}